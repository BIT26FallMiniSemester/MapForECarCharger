# 验证范围

验证日期：2026-09-12。

- Python 导出/MapReduce：18 项测试通过，包括 SQLite 独立查询核对、跨日边界、全部状态、无效站点、空数据和并发快照。
- Hadoop 3.2.1 + Java 8 单机 HDFS/YARN：真实空订单快照作业及 23 笔合成订单作业成功；非空作业启动 2 个 Mapper 和 2 个 Reducer，处理结果通过独立 SQLite 查询核对。测试订单不写入业务数据库。
- Qt6/CMake：编译成功，原有 charger-tests 和新增 analytics-tests 通过。
- Qt HTTP：在数据库临时副本上验证实时数据、Hadoop 结果、过期、损坏、缺失和恢复响应。
- Vue：5 项数据合并测试和 Vite 生产构建通过。
- Qt/Vue 浏览器联调：地图显示 2614 个站点及 32256 个电桩；通过 TCP 接口预约后页面自动显示已预约及空闲桩减 1，取消后恢复。只操作预览副本，测试预约已取消。地图常驻站点名称已隐藏。

Hadoop 批次结果经真实 Qt HTTP 验证，但浏览器中成功加载 Hadoop 批次的确认尚未完成。当前未启用定时调度；单机小样本验证不代表多节点容错或大规模性能验证。

复现方式见 README.md；本地数据库、SSH 密钥、运行日志和临时产物不属于源码交付。
