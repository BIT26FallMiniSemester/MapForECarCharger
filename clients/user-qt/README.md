# Qt 用户端

用户端通过带长度头的 JSON TCP Socket 连接 `server-qt`，不直接访问 SQLite，也不保存腾讯地图 Key。

## 构建和运行

```bash
cd ~/MapForECarCharger/clients/user-qt
qmake6 charging-user-client.pro
make -j$(nproc)
./charging-user-client
```

默认后端为 `127.0.0.1:9000`。演示账号：

- `13900000000`：正常车主，可完成一笔新订单；
- `13600000000`：已有充电中订单，用于测试断线恢复和实时计费；
- `13500000000`：已有待支付订单；
- `13800000000`：冻结用户，用于异常流程。

## 演示流程

1. 登录 `13900000000`，在“我的”页面按需充值。
2. 首页修改位置；可选北京区域，也可输入地址由后端调用腾讯地图解析。
3. 查询附近站点，列表按腾讯地图驾车路线距离排序。
4. 点击“选择电桩”，查看路线时间并选择空闲电桩。
5. 客户端创建订单并自动预约；用户点击“开始充电”。
6. “充电”页每两秒向后端查询时长、电量和金额。
7. 结束充电生成待支付账单，再使用余额付款。
8. 重新打开客户端或切换到“充电”页，会从后端恢复未完成订单。
