# ML 功能验收 — 2026-09-12

范围仅为 ml/，未修改、启动或写入后端。图片中的 12 项功能与运行入口逐项列在 [README](README.md)。

## 修复与补齐

- 原先只有 +1/+6/+24 三个单点；现在输出连续 24 小时，并按接口契约组成 1/6/24 小时窗口。
- 修正星期编号导致的周末判断错误；日历特征明确使用 Asia/Shanghai。
- 修正时间切分中未来标签跨越训练/验证边界的问题，保留边界元数据作为检查证据。
- 增加模拟订单/设备日志生成、清洗质量报告、显式节假日日历和运营特征。
- 增加距离/空闲率/预测拥堵联合推荐、高峰与设备异常提示、留出集对比图和误差分析。
- 增加非有限数、非法字段、模型损坏、预测缺点、映射缺失和分页无进展的检查。
- 核心训练和预测已从 C++17 迁移为 Python V4；V3 模型可读取，V2 需要重训。

## 已运行检查

- Python unittest：10 项通过，包含完整离线工作流和模拟 HTTP 导出/写入。
- Python：源码编译检查、独立 demo 和 10 项 unittest 通过。
- 真实 HTTP 联调：临时 SQLite 后端完成目录映射、9 个窗口上传和公开接口读回；重复上传后仍为 279 个类型化预测点，幂等。
- 迁移前全量存档：2,605 个有效站点，62,520 个逐小时预测点；本次用同一 V3 模型抽查 1 个站点，Python 的 24/24 个预测点与原 C++ 输出一致。
- 迁移前后端请求体存档：7,815 个窗口请求、242,265 个类型化点；每站窗口分别含 3/18/72 个点。
- 全量检查：预测时间每小时连续，负荷不超过站点容量，可用桩不小于 0 且不超过总桩数。
- 测试集对比图已生成并打开检查，见 [evaluation.svg](models/evaluation.svg)；图只代表一个测试站点。
- 推荐示例：位置 39.9,116.4、20 km 半径下得到 1,366 个候选；401 个高负荷告警，非用户实时位置/实时业务告警。

## 重现

```bash
python3 -m unittest discover -s ml/tests -p 'test_*.py' -v
python3 ml/src/workflow.py demo ml/outputs/new-check
```

## 限制

- 本次迁移验收环境是 macOS arm64、Python 3.14.7；不再要求 C++ 编译器。
- 本次未重新训练 2,605 个站点的全量模型；仓库保留原 V3 模型和全量产物，Python 可直接读取，重新训练将生成 V4 JSON 模型。
- 未使用生产密钥或生产数据库；真实 HTTP 联调只写入本机临时 SQLite 数据库。
- 模型效果只在模拟负荷上验证；当前空闲桩/拥堵换算与设备告警是明确记录假设的基线，不是独立训练的故障预测模型。
- 清洗依赖完整订单导出；未知设备状态保守计为不可用；真实节假日日历须单独提供。
- 提交与同步状态以 Git 历史为准，目标分支为 feature/backend-ml-station-mapping。
